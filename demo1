export const HomeAppQrModal: React.FC<HomeAppModalProps> = ({ onClose, show }) => (
  <Modal
    centered
    aria-labelledby="contained-modal-title-vcenter"
    show={show}
    onHide={onClose}
    className="modal-dialog-centered"
  >
    <ModalHeader closeButton className="border-0"></ModalHeader>
    <HomeAppModalBody>
      <HomeAppQrModalTitle>Scan the QR code to download the Verizon Home app.</HomeAppQrModalTitle>{" "}
      <img alt="home-app" src={require(`../../../assets/images/VerizonHomeApp.png`)} style={HomeQrImgStyle} />
    </HomeAppModalBody>
  </Modal>
);

const HomeAppModalBody = styled(VZModalBody)`
  display: flex;
  flex-direction: column;
  align-items: center;
  padding-top: 0;
`;
I am using react-bootstrap
I want to make the border-radius of this 8px and also want to change the font weight of the close button to be 300

type BackhaulLogModel = {
    time: string;
    date: string;
    backhaulType: BackhaulType;
    connectionRate: string;
    macAddress?: string | undefined;
}
export const parseBackhaulLogApiResponse = (data: string): BackhaulLogModel[] => {
  if (data.length === 0) {
    return [];
  }
  const lineSeparator = "\u2551";
  const valueSeparator = "\u2588";
  const trimmedData = data?.trim();
  const splitList = trimmedData
    ?.split(lineSeparator)
    ?.slice(0, trimmedData?.[trimmedData?.length - 1] === lineSeparator ? -1 : undefined);
  return splitList
    .map((val) => {
      const splitVal = val?.split(valueSeparator);
      const dateTimeString = splitVal?.[0]?.trim();
      if (!dateTimeString || isNaN(Date?.parse(dateTimeString))) {
        return [];
      }
      const dateTime = new Date(dateTimeString);
      const date = dateTime
        ?.toLocaleDateString("en-US", { year: "2-digit", month: "2-digit", day: "2-digit" })
        .replace(/\//g, " / ");
      const time = dateTime
        ?.toLocaleTimeString("en-US", { hour: "2-digit", minute: "2-digit", hour12: true })
        .replace(/\u200E/g, "")
        .toLowerCase();
      return {
        date: date,
        time: time,
        type: convertStringToBackhaulType(splitVal?.[1]?.trim()),
        connectionRate: splitVal?.[2]?.trim(),
        macAddress: splitVal?.[3]?.trim(),
      };
    })
    .filter((entry) => entry !== null);
};

export const convertStringToBackhaulType = (string: string): BackhaulType => {
  switch (string) {
    case "0":
      return BackhaulType.Ethernet;
    case "1":
      return BackhaulType.Wifi;
    case "2":
      return BackhaulType.Coax;
    default:
      throw new Error(`Unsupported BackhaulType ${string}`);
  }
};


the errror i am getting 

Type '(never[] | { date: string; time: string; type: BackhaulType; connectionRate: string; macAddress: string; })[]' is not assignable to type 'BackhaulLogModel[]'.
  Type 'never[] | { date: string; time: string; type: BackhaulType; connectionRate: string; macAddress: string; }' is not assignable to type 'BackhaulLogModel'.
    Type 'never[]' is missing the following properties from type 'BackhaulLogModel': time, date, backhaulType, connectionRate

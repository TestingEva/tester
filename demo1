type ParentalControlDeviceInfo = {
  deviceName: string;
  macAddress: string;
  deviceType: DeviceType;
};

deviceInfo is of type ParentalControlDeviceInfo[] and 

parentalData is of type ParentalRule[]

type ParentalRule = {
  id: string;
  name: string;
  schedules: ScheduleInfo[];
  enabled: boolean;
  devices: ParentalControlDeviceInfo[];
  websiteExceptions: string[];
  keywordExceptions: string[];
  internetAccessible: boolean;
};


we need to show only those devices which are not alloted any parental Rule


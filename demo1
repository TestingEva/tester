export function useMsg() {
  const profileMode = useUserProfileModeInfo();
  const platInfo = usePlatformInfo();
  const query = useQuery({
    queryKey: ["KEY_CONTENT"],
    queryFn: (id) =>
      getContent(
        id,
        UserProfileMode[profileMode.data!] as keyof typeof UserProfileMode,
        PlatformModel[platInfo.data!.model] as keyof typeof PlatformModel
      ),
    enabled: platInfo.data?.modelName !== undefined && profileMode.data !== undefined,
  });
  return query;
}


Hi i want to design this such that this query returns a function and i can pass id to that function 


usage example  -

const {tempFun} = useMsg()

const name = tempFun("nameID")

Like for how we can check the input feilds are protected Cross-Site Scripting (XSS)

This was the common input which i have used throughout my page

import React from "react";
import "./VZInput.css";

export const VZInputs = ({
    className,
    type = "text",
    value,
    onChange,
    disable = false,
    maxLength,
    placeHolder = "",
}) => {
    return (
        <div>
            <input
                maxLength={maxLength}
                className={` h ${className}  ${disable ? "disabled_input" : "input_field"
                    }`}
                type={type}
                value={value}
                onChange={onChange}
                disabled={disable}
                placeholder={placeHolder}
            />
        </div>
    );
};

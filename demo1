 startTime: z.string().refine((val) => val.includes(":")),

Hi I want to verify the schema using ZOD for a string whose format will be "HH:mm" where HH can be 00 to 23 and mm can be 00 to 59 

also for here   days: z.array(z.number().min(0).max(6)), I want to check the length of the array it should not be more than 7 and it should not be empty

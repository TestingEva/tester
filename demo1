import { Dayjs } from "dayjs";
import { useState } from "react";
import { DayPicker, getDefaultClassNames } from "react-day-picker";
import "react-day-picker/style.css";

type DatePickerProps = {
  selectedDate?: Dayjs;
  onDaySelect?: (selectedDate: Dayjs) => void;
};

export const DatePickerComponent: React.FC<DatePickerProps> = (props) => {
  const defaultClassNames = getDefaultClassNames();
  const [selected, setSelected] = useState<Date>();

  return (
    <DayPicker
      mode="single"
      selected={selected}
      onSelect={setSelected}
      captionLayout="dropdown"
    />
  );
};


Hi could you help me in styling this component I want the selected day color white and background of the selected day to be black and it should be having 0 boreder radius

I have a startHour in 12 hour format startMinutes in 00 to 60 mins and also a AM or PM 

i want you to write a fn in typeScript which will take this three input and return a Dayjs of these


import dayjs from 'dayjs';

function createDayjsTime(startHour: number, startMinutes: number, period: 'AM' | 'PM'): dayjs.Dayjs {
    // Convert 12-hour format to 24-hour format
    const hour24 = period === 'PM' && startHour !== 12 ? startHour + 12 : (period === 'AM' && startHour === 12 ? 0 : startHour);

    // Create a Dayjs object with the converted time
    return dayjs().hour(hour24).minute(startMinutes).second(0);
}

// Example usage
const time = createDayjsTime(10, 30, 'PM');
console.log(time.format()); // Output: current date with 22:30:00 time

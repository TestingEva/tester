
export const DnsServerView: React.FC<Props> = ({ data }) => {
  return (
    <>
      <VZHeader
        title="DNS Server"
        breadcrumbData={[{ label: "Network Settings" }, { label: "DNS Server", destType: RouteType.DNS_SERV }]}
      />
      <VZBody noTopPadding>
        <DnsServerDecs>Add, edit, or delete computers known by the router's DNS server</DnsServerDecs>
        <Table>
          <thead className="b">
            <DnsServerTableRow className="border-0">
              <td className="col-3 ps-0">Host Name</td>
              <td className="col-3">IP Address</td>
              <td className="col-4">Source</td>
              <td className="col-2"></td>
            </DnsServerTableRow>
          </thead>
          <tbody>
            {data.dnsServerList.map((e) => (
              <DnsServerTableRow key={`${e.objPath}`}>
                <td className="col-3 ps-0">{e.hostname}</td>
                <td className="col-3">{e.ipAddress}</td>
                <td className="col-3">{convertDnsSourceToUIString(e.source)}</td>
                <td className="col-2">
                  <Row>
                    <DnsServerTableActions>Edit</DnsServerTableActions>
                    <DnsServerTableActions className="ps-0 ">Remove</DnsServerTableActions>
                  </Row>
                </td>
              </DnsServerTableRow>
            ))}
          </tbody>
        </Table>
      </VZBody>
    </>
  );
};

Hi I as you see I have the table actions in which the property is 

export const DnsServerTableActions = styled(Col)`
  text-decoration: underline;
  text-align: end;
  &:hover {
    cursor: pointer;
  }
`;

now whenever i want whenever i hover over the text edit then only the cursor should change to pointer but now currently with this code 
cursor remains in pointer throughout the col width 

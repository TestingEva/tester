HI ,
   buildNavRouteInfo("basic/wifi/primary", ContentMode.Basic, RouteType.PRIMARY_NW, {
      label: `${provider(MSG_YES)}`,
      groupLabel: "Wi-Fi",
    }),
I have the above function 

export function buildNavRouteInfo(
  path: string,
  filter: ContentMode,
  type: RouteType,
  navInfo: { label: string; groupLabel?: string },
  isProtected = true,
  isEmbedded = true
) {
  return new NavRouteInfo(type, path, isProtected, filter, navInfo.label, isEmbedded, navInfo.groupLabel);
}

this is its desc


now provider is used which is causing error 

I have imported properly and passed provider 

export function useMessageProvider(): MessageProvider {
  const client = useQueryClient();

  const provider = useRef((id: string, templateParams?: Record<string, string>) => {
    const profileMode = client.getQueryData([KEY_USER_PROFILE_MODE]) as UserProfileMode;
    const platInfo = client.getQueryData([KEY_PLATFORM_INFO]) as PlatformInfo;
    let initialValue = getContent(id, profileMode, platInfo);

    if (templateParams !== undefined) {
      for (const key in templateParams) {
        initialValue = initialValue.replaceAll(`{${key}}`, templateParams[key]);
      }
    }
    return initialValue;
  });

  return provider.current;
}

its some type script error cannot read the property of undefined 

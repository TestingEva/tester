export function useLoginMetadata(): AsyncHookData<AuthMetadata> {
  const { status, data, error } = useQuery({
    queryKey: [KEY_LOGIN_METADATA],
    queryFn: getLoginMetadata,
  });
  return asyncHookData(status, error, data);
}
this is a function in my react Project in which i am using React Query 



export async function getLoginMetadata(): Promise<AuthMetadata> {
  const response = await getLoginMetadataApi();
  return {
    passwordLess: !response.passwordRequired,
    showRouterRearImage: !response.adminPwdChanged,
  };
}

export async function getLoginMetadataApi(): Promise<LoginMetadataResponse> {
  try {
    const response = await httpRequest(LOGIN_META_DATA_URL).post().readAsJson();
    return LoginMetadataResponseSchema.parse(response) as LoginMetadataResponse;
  } catch (e) {
    mapAndThrowError(e);
  }
}

the httpRequest is wretch 

now i want you to write the test cases for this 
use fetch-mock-jest to mock the server 

const LOGIN_META_DATA_URL = `http://localhost:3004/CRSP/getLoginMetadata`;

Use all the above information and complete this task


This was my component

<ReactSlider
                  className="customSlider"
                  trackClassName="customSlider-track"
                  thumbClassName="customSlider-thumb"
                  markClassName="customSlider-mark"
                  min={0}
                  max={100}
                  step={50}
                  value={data.brightnessPercentage}
                  onChange={onBrightnessChange}
                />

Imported from from "react-slider": "^2.0.6",

but it is not supported in React 19
 
 so I am planning to move to 

 rc-slider

another npm package


I have led the foundation of the same <Slider min={0} max={100} step={50} />

I am able to see the slider with the steps 

now i want you to style the new slider to look as the old one 

it should exactly look as the old one 

giving you the css for the old Slider below


.customSlider {
  max-width: 12.5rem;
  margin-left: 0%;
}

.customSlider-track {
  top: 0.5rem;
  height: 0.125rem;
  background: black;
}

.customSlider-thumb {
  background: white;
  width: 1.25rem;
  height: 1.25rem;
  border-radius: 100%;
  outline: black solid 1px;
}

.customSlider-mark {
  top: 0.375rem;
  width: 0.01rem;
  height: 0.5rem;
  background-color: black;
}


Please make sure you do  PROPER JOB



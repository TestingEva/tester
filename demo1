Argument of type 'string | number | boolean | Dayjs | undefined' is not assignable to parameter of type 'never'.
  Type 'undefined' is not assignable to type 'never'.

getting the above error from the following code 
import { NetworkConnectionsInfo } from "../../../domain/system-wide-connections/models";

type NwConnUIInfo = {
  [K in keyof NetworkConnectionsInfo]: NetworkConnectionsInfo[K][];
};

export function covertNwConnInfoToNwConnUIInfo(data: NetworkConnectionsInfo[]): NwConnUIInfo {
  const result = {} as NwConnUIInfo;
  data.forEach((item) => {
    Object.keys(item).forEach((key) => {
      const nwKey = key as keyof NetworkConnectionsInfo;
      if (!result[nwKey]) {
        result[nwKey] = [];
      }
      result[nwKey].push(item[nwKey]);// error comes from here
    });
  });
  return result;
}

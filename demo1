export function convertStringToSystemLogs(data: string, isMigrationLog: boolean): SystemLogModel[] {
  if (data.length === 0) {
    return [];
  }
  const lineSeparator = "\u2551";
  const valueSeparator = "\u2588";
  const trimmedData = data.trim();
  const splitList = trimmedData
    .split(lineSeparator)
    .slice(0, trimmedData[trimmedData.length - 1] === lineSeparator ? -1 : undefined);
  return splitList.map((val) => {
    const splitVal = val.split(valueSeparator);
    if (splitVal.length > 0) {
      if (isMigrationLog) {
        return {
          dateTime: dayjs(splitVal[0].trim(), DATE_FMT_HMS),
          eventType: "[MIGRATION]",
          level: SystemLogLevel.Info,
          detail: splitVal[1].trim(),
        };
      }
      return {
        dateTime: dayjs(splitVal[0].trim(), DATE_FMT_HMS),
        eventType: splitVal[1].trim(),
        level: stringToLogLevel(splitVal[2].trim()),
        detail: splitVal[3].trim(),
      };
    }
  });
}

Hi the problem is I have added the length check on splitVal and map doesnot takes undefined how to solve this issue

export type PortForwardingData = {
  enabled: boolean;
  groupExternalPortRange: ([number] | [number, number])[];
  hostName: string;
  internalClient: string;
  internalPort: number;
  name: string;
  portMappingId: string;
  protocol: PortForwardingProtocolData;
  scheduleId: string;
};

export enum PortForwardingProtocolData {
  Tcp,
  Udp,
  Both,
  Gre,
  Esp,
  Ah,
}

I guess then you need to correct the Models also for the groupExternalPortRange

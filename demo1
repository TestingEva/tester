export const VZSmallButton = styled.button<{
  $primary?: boolean;
  $disabled?: boolean;
}>`
  min-width: 0;
  height: min-content;
  border-radius: 100px;
  border: 1px solid #000;
  padding: 5px 0;
  width: 5.5rem;
  vertical-align: middle;
  background-color: ${(props) => (props.$disabled ? "#d8dada" : "white")};
  cursor: ${(props) => (props.$disabled ? "select" : "pointer")};
  font-size: 13.33px;
  font-family: "Verizon NHG eTX Bold", sans-serif;
  ${(props) => props.$primary && primaryButtonStyle}
`;

        <VZSmallButton $primary $disabled={true}>
          Save
        </VZSmallButton>

my button is not getting the properties which i am applying on disable

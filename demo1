export type UpdateWifiDetailApiRequest = {
  networkType: 0 | 1 | 2;
  sonEnabled?: boolean;
  wifiInfoList?: Update<WifiBandData, "radioAlias">[];
};

export type WifiBandData = {
  ssid: string;
  key: string;
  enabled: boolean;
  securityType: number;
  ssidBroadcastEnabled?: boolean;
  radioAlias: string;
  radioEnabled: boolean;
  wmmConfig?: {
    enabled: boolean;
    powersaveEnabled: boolean;
    isReadOnly: boolean;
  };
  macAddressControl?: {
    enabled: boolean;
    isWhitelist: boolean;
    addresses: string[];
  };
  groupKeyConfig?: {
    enabled: boolean;
    interval: number;
  };
};

Create a function which will take UpdateWifiDetailApiRequest as payload and need to check whether update request have
sonEnabled not as undefined or have radioAlias 6Ghz present in it
response of the function should be boolean



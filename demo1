setValue(e.replace(/[^0-9a-fA-F:]/, ""));

so i have the regex which accepts valid ipv6 

it works fine when i am entering single character but it fails when i paste multiple characters 

suppose i enter fe80:zxyl the value set is fe80:xyl but it should be allowed  could you fix the regex 
